services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      DOCKER_IMAGE: ${DOCKER_IMAGE:-your-dockerhub-user/docker-ssr-hello:latest}
      PROJECT_DIR: ${PROJECT_DIR:-may/26mayDockerSSR}
      OFFLINE_IMAGE: ${OFFLINE_IMAGE:-docker-ssr-hello:offline}
      OFFLINE_TAR: ${OFFLINE_TAR:-output/docker-ssr-hello.tar}
